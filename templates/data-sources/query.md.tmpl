---
page_title: "{{.Type}} {{.Name}} - {{.ProviderName}}"
subcategory: "Utilities"
description: |-
  Runs a read-only SQL query and returns the rows.
---

# {{.Type}} ({{.Name}})

Runs a read-only SQL query and returns the rows. Use it to read PostgreSQL catalog tables, or to run your own read-only queries.

~> **Security:** By default this data source accepts only `SELECT` queries, including CTEs that start with `WITH`. Writes such as `INSERT`, `UPDATE`, and `DELETE`, and schema changes such as `CREATE`, `DROP`, and `ALTER`, are **rejected**. Every query also runs inside a **read-only transaction**, so PostgreSQL blocks any write even if a query gets past that first check. To run destructive queries, you must set `allow_destructive = true`.

## Result Format

Each row comes back as a map of string to string under the `rows` attribute. Numbers, timestamps, booleans, and `NULL` all use PostgreSQL's default text form, so convert them in Terraform when you need to, with `tonumber`, `toset`, and so on.

```terraform
# Access a single column:
value = data.postgresql_query.example.rows[0]["column_name"]

# Coerce a numeric column:
count = tonumber(data.postgresql_query.example.rows[0]["total"])

# Extract a list of values:
names = [for r in data.postgresql_query.example.rows : r["name"]]
```

## Common Usage Patterns

### Query catalog tables

```terraform
data "postgresql_query" "database_size" {
  database = "postgres"
  query    = "SELECT pg_size_pretty(pg_database_size('myapp')) AS size"
}

output "db_size" {
  value = data.postgresql_query.database_size.rows[0]["size"]
}
```

### Run an idempotent hardening script (destructive)

```terraform
data "postgresql_query" "revoke_public" {
  database          = "myapp"
  allow_destructive = true
  query             = <<-SQL
    REVOKE CONNECT ON DATABASE myapp FROM PUBLIC;
    REVOKE CREATE  ON SCHEMA   public FROM PUBLIC;
  SQL
}
```

~> **`allow_destructive = true` runs in a writable transaction.** Statements that cannot run inside a transaction, such as `CREATE DATABASE` and `VACUUM`, fail here too. Run those outside Terraform.

## Example Usage

{{tffile "examples/data-sources/postgresql_query/data-source.tf"}}

{{ .SchemaMarkdown | trimspace }}
