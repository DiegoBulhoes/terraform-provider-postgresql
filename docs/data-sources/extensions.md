---
page_title: "Data Source postgresql_extensions - terraform-provider-postgresql"
subcategory: ""
description: |-
  Lists installed PostgreSQL extensions.
---

# Data Source (postgresql_extensions)

Lists installed PostgreSQL extensions with their version, schema, and description.

## Example Usage

```terraform
# List all installed extensions
data "postgresql_extensions" "all" {}

output "installed_extensions" {
  value = {
    for ext in data.postgresql_extensions.all.extensions :
    ext.name => ext.version
  }
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `database` (String) The database to query. Uses the provider default if not set.

### Read-Only

- `extensions` (Attributes List) List of installed extensions. (see [below for nested schema](#nestedatt--extensions))

<a id="nestedatt--extensions"></a>
### Nested Schema for `extensions`

Read-Only:

- `description` (String) A description of the extension.
- `name` (String) The name of the extension.
- `schema` (String) The schema the extension is installed in.
- `version` (String) The installed version of the extension.
